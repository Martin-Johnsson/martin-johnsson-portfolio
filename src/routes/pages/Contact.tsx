import { FC } from 'react';
import ContactForm from '../../components/ContactForm';

const Contact: FC = () => {
  return (
    <main>
      <ContactForm />
    </main>
  );
};

export default Contact;
